import { styled } from "styled-components";
import { m } from "framer-motion";

export const Wrapper = styled(m.main)`
    max-width: 790px;
    width: 100%;
    margin: 0 auto;
    display: flex;
    align-items: center;
    column-gap: 3rem;
    flex: 1;
`;
